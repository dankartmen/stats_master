Генератор статистических распределений и байесовский классификатор
Приложение для генерации и визуализации статистических распределений методом обратного преобразования, а также для классификации образов с использованием байесовского метода. Разработано в рамках задания по математической статистике.
📊 ## О проекте
Приложение реализует:

Генерацию случайных величин, распределенных по биномиальному, равномерному и нормальному законам.
Байесовский классификатор для двух классов с заданными распределениями и априорными вероятностями.

Используются методы обратного преобразования через функцию распределения и аналитические методы байесовской классификации.
🎯 ## Возможности
Часть 1: Генератор распределений
Поддерживаемые распределения:

Биномиальное распределение (дискретное).
Равномерное распределение (непрерывное).
Нормальное распределение (непрерывное).

Основной функционал генератора:

Ввод параметров распределения для каждого типа.
Настройка размера выборки: генерация от 1 до 1000+ значений.
Три режима просмотра результатов:
Столбчатая диаграмма распределения.
Таблица частот/интервальный ряд с процентами.
Сетка сгенерированных значений.

Интерактивное объяснение: при нажатии на значение показывается подробная информация о процессе генерации.
Сохранение и загрузка результатов генерации.
Комплексная оценка параметров всех распределений с интервальными оценками.

Часть 2: Байесовский классификатор образов
Настройка классификатора:

Выбор распределений для двух классов (нормальное, равномерное, биномиальное).
Задание априорных вероятностей классов.
Ввод параметров распределений для каждого класса.
Настройка названий классов.

Расчет и визуализация:

График плотностей распределений, умноженных на априорные вероятности.
Определение точек пересечения плотностей (границ решений).
Расчет теоретической вероятности ошибки классификации.
Табличное представление вклада интервалов в общую ошибку.

Экспериментальная проверка:

Генерация тестовых данных в соответствии с априорными вероятностями.
Классификация тестовой выборки и расчет эмпирической ошибки.
Сравнение теоретической и эмпирической ошибок.
Матрица ошибок (confusion matrix) и статистики по классам.

Детальный анализ:

Пошаговый расчет ошибки для каждого интервала с формулами.
Просмотр деталей классификации отдельных значений.
Интерактивная проверка классификации на тестовых значениях.

📚 ## Теоретические основы
Метод Монте-Карло
В решении использовался метод Монте-Карло — это численный метод, который позволяет изучать случайные процессы с помощью генератора случайных величин.
Случайная величина — число, конкретное значение которой проявляется в опыте.
Закон распределения представлен рядом распределения:
$$\xi: x_1, x_2, x_3, \dots, x_n$$
$$P: p_1, p_2, p_3, \dots, p_n$$
где:

$\xi$ — случайная величина,
$P$ — вероятность.

Распределения генератора
Биномиальное распределение
$$\xi: 0, 1, 2, \dots, n$$
$$P: p_0, p_1, p_2, \dots, p_n$$
$$P(\xi = m) = \binom{n}{m} p^m (1 - p)^{n - m}$$
где:

$\xi$ — число успехов в $n$ независимых испытаниях,
$\binom{n}{m}$ — биномиальный коэффициент,
$p$ — вероятность успеха в одном испытании,
$n$ — количество испытаний,
$m$ — количество успехов ($0 \leq m \leq n$).

Равномерное распределение
$$f(x) = \frac{1}{b - a} \quad \text{для } x \in [a, b]$$
$$f(x) = 0 \quad \text{для } x \notin [a, b]$$
где:

$a$ — нижняя граница распределения,
$b$ — верхняя граница распределения.

Нормальное распределение
$$\xi \sim N(m, \sigma^2)$$
$$f(x) = \frac{1}{\sigma \sqrt{2\pi}} \exp\left( -\frac{(x - m)^2}{2\sigma^2} \right)$$
где:

$m$ — математическое ожидание,
$\sigma$ — стандартное отклонение.

Байесовский классификатор
Решающее правило
Байесовский классификатор относит наблюдение $x$ к классу $\omega_1$, если выполняется неравенство:
$$P(\omega_1) \cdot f_1(x) \geq P(\omega_2) \cdot f_2(x)$$
где $P(\omega_i)$ — априорная вероятность класса $\omega_i$, а $f_i(x)$ — функция плотности распределения класса $\omega_i$.
Теоретическая ошибка
Теоретическая вероятность ошибки классификации:
$$P_{\text{error}} = \int_{-\infty}^{\infty} \min \left[ P(\omega_1) \cdot f_1(x), P(\omega_2) \cdot f_2(x) \right] \, dx$$
Используемые параметры распределений

Нормальное распределение: $m = 5$, $\sigma = 1$; $M\xi = 5$, $D\xi = 1$.
Равномерное распределение: $a = 3$, $b = 5$; $M\xi = 4$, $D\xi = \frac{1}{3}$.
Биномиальное распределение: $n = 10$, $p = 0.32$; $M\xi = 3.2$, $D\xi = 2.176$.

⚙️ ## Алгоритмы
Генерация распределений
Биномиальное распределение

Вычисление кумулятивных вероятностей: $a_0, a_1, \dots, a_n$.
Генерация случайного числа $u \in [0, 1]$.
Определение интервала: $a_{k-1} < u \leq a_k$.
Возврат значения $k$.

Равномерное распределение

Генерация случайного числа $u \in [0, 1]$.
Преобразование: $x = a + u \times (b - a)$.
Возврат значения $x$.

Нормальное распределение

Генерация 12 независимых случайных чисел $u_1, u_2, \dots, u_{12}$ (равномерного распределения).
Вычисление стандартной нормальной величины: $z = (\sum u_i) - 6$.
Преобразование: $x = z \times \sigma + m$.
Возврат значения $x$.

Байесовская классификация
Нахождение точек пересечения

Определение области анализа на основе параметров распределений.
Дискретный перебор с заданным шагом для нахождения интервалов смены знака разности плотностей.
Уточнение точек пересечения методом бисекции.

Расчет теоретической ошибки

Разбиение области интегрирования на интервалы точками пересечения.
На каждом интервале определение "проигрывающего" класса (с меньшей взвешенной плотностью).
Интегрирование плотности распределения "проигрывающего" класса на интервале и умножение на его априорную вероятность.
Суммирование ошибок по всем интервалам.

Аналитические методы интегрирования:

Нормальное распределение: Использование таблицы функции Лапласа

$$\int_a^b f(x) \, dx = \Phi\left( \frac{b - m}{\sigma} \right) - \Phi\left( \frac{a - m}{\sigma} \right)$$

Равномерное распределение: Аналитический расчет площади прямоугольника

$$\int_a^b f(x) \, dx = \frac{\min(b, B) - \max(a, A)}{B - A}$$
где $[A, B]$ — носитель распределения.
Генерация тестовых данных

Объем выборки для каждого класса определяется пропорционально априорным вероятностям.
Для генерации используются алгоритмы из лабораторных работ 1-3.

🖥️ ## Интерфейс

Экран выбора распределения (DistributionSelectionScreen):
Карточки для выбора типа распределения.
Описание каждого распределения.
Визуальные индикаторы для дискретных/непрерывных распределений.
Кнопка "Комплексная оценка параметров" для перехода к оценке всех распределений.
Кнопка "Байесовский классификатор" для перехода к классификатору.

Экран параметров (ParametersScreen):
Поля ввода специфичные для каждого типа распределения:
Биномиальное: $n$ (количество испытаний), $p$ (вероятность успеха).
Равномерное: $a$ (нижняя граница), $b$ (верхняя граница).
Нормальное: $m$ (математическое ожидание), $\sigma$ (стандартное отклонение).

Поле ввода размера выборки.
Кнопка генерации значений.

Экран комплексной оценки (AllParametersScreen):
Ввод параметров для всех трех распределений одновременно.
Настройка индивидуальных размеров выборки для каждого распределения.
Расчет точечных и интервальных оценок.

Экран результатов оценки (AllParameterEstimationScreen):
Сравнительная таблица теоретических и выборочных характеристик:
Математическое ожидание.
Дисперсия.
Исправленная дисперсия.
Стандартное отклонение.

Интервальные оценки для нормального распределения:
Доверительные интервалы для математического ожидания ($\sigma$ известна и неизвестна).
Доверительные интервалы для дисперсии.
Ширина интервалов и центры.

Визуализация различий между теоретическими и выборочными значениями.

Экран настройки классификатора (BayesianClassifierScreen):
Ввод априорных вероятностей для двух классов.
Задание названий классов.
Выбор типа распределения и ввод параметров для каждого класса.
Кнопка запуска классификации.

Экран результатов классификации (BayesianResultsScreen):
Карточка параметров классификатора: отображение заданных параметров и точек пересечения.
График плотностей распределений: визуализация $P(\omega_1) \cdot f_1(x)$ и $P(\omega_2) \cdot f_2(x)$ с точками пересечения.
Правило классификации: текстовое представление решающего правила.
Тестирование классификатора:
Кнопки для проверки классификации на нескольких значениях и запуска полного теста.
Отображение теоретической вероятности ошибки.
Отображение экспериментальной ошибки после теста.
Кнопки для просмотра детальных результатов и значений.

Детали расчета теоретической ошибки: таблица интервалов с вкладом в ошибку. Возможность нажать на значение ошибки для просмотра подробного расчета с формулами и подстановкой значений.

Диалоговые окна детализации (классификатор):
Детали теоретической ошибки: таблица интервалов, формула расчета, пошаговое вычисление для каждого интервала.
Детальные результаты тестирования: матрица ошибок, статистика по классам, список классифицированных образцов.
Проверка классификации: отладка классификации на конкретных значениях с выводом плотностей и принятого решения.

Режимы просмотра результатов генерации:
Для биномиального распределения:
Вкладка "График" — столбчатая диаграмма эмпирического распределения.
Вкладка "Таблица" — таблица частотного распределения.
Вкладка "Значения" — сетка сгенерированных значений.

Для непрерывных распределений (равномерное и нормальное):
Вкладка "Гистограмма" — гистограмма плотности распределения.
Вкладка "Интервальный ряд" — таблица интервального вариационного ряда.
Вкладка "Значения" — сетка сгенерированных значений.



🔧 ## Технические особенности
Архитектура

BLoC-паттерн для управления состоянием приложения.
Навигация через Navigator.
Repository-паттерн для работы с данными.

Алгоритмические оптимизации

Итеративное вычисление биномиальных коэффициентов (без факториалов).
Бинарный поиск для определения интервалов (O(log n)).
Центральная предельная теорема для генерации нормального распределения.
Автоматическое определение интервалов для непрерывных распределений.
Статистические расчеты для точечных и интервальных оценок.
Аналитический расчет интегралов для нормального и равномерного распределений.
Таблицы функции Лапласа для быстрого и точного вычисления интегралов нормального распределения.
Метод бисекции для уточнения точек пересечения.
Пропорциональная генерация тестовых данных в соответствии с априорными вероятностями.

Виджеты и визуализация

BarChart из библиотеки fl_chart для диаграмм.
LineChart из библиотеки fl_chart для графиков плотностей.
TabBar с тремя вкладками для навигации.
GridView для отображения значений.
AlertDialog для показа детальной информации.
LinearProgressIndicator для визуализации процентов.
Сравнительные таблицы для оценок параметров.
Интерактивные элементы (кликабельные ячейки таблиц для детализации).
Анимации для плавного появления элементов.

🔗 ## Связь между компонентами
Проект объединяет генератор распределений и байесовский классификатор в единое приложение:

Генератор распределений используется для создания тестовых данных при экспериментальной проверке классификатора.
Методы расчета плотностей распределений используются как в генераторе, так и в классификаторе.
Визуализационные компоненты переиспользуются для отображения гистограмм и графиков.